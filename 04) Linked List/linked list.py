#if user chooses Linked List (illustration)

elif structure == "Linked List":
    ll = linkedlist()
    for ll in parsed:
        ll.append(parsed)
    print(f"Demonstation of LinkedList: {ll.display()}")

#note: the blueprint must be declared before running the syntax